import React from 'react';

function GenerateFilters({
    minUnits,
    setMinUnits,
    maxUnits,
    setMaxUnits,
    avoidTimes,
    setAvoidTimes
}) {
    return (
        <p>Filters</p>
    );
}

export default GenerateFilters;